/**
 * Barrel file for all NgRx `state` definitions for this application. When you
 * add new one just add it here and it will be usable everywhere in this
 * application via that main NgRx store barrel file.
 */

export * from 'src/app/store/authentication/authentication.state';
export * from 'src/app/store/error/error.state';
export * from 'src/app/store/layout/layout.state';
export * from 'src/app/store/version/version.state';
